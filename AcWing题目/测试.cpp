# include <iostream>
# include <string>
# include <vector>
using namespace std;

int main() {

	string a[100];
	int n, m, sum = 1;

	while (cin >> n >> m) {

		for (int i = 0; i < sizeof(a) / sizeof(string); i++) {

			cin >> a[i];

			if ()
		}
	}

	return 0;
}